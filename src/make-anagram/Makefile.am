# -*- mode: makefile -*-

include $(top_srcdir)/Makefile.common

bin_PROGRAMS = t

t_SOURCES = t.cpp
t_CPPFLAGS = $(AM_CPPFLAGS) $(BENCHMARK_CPPFLAGS)
t_LDADD = $(AM_LDFLAGS) $(LIBS) $(BENCHMARK_LIBS)
